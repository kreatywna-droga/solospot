# G1-56 Product Selection

- **Selected Capability**: `PageBuilderCanvasRuntimeAdapter.ts`
- **Category**: Canvas Runtime & UI Integration Adapter
- **Rationale**: Connects the visual page builder interaction subsystem to real canvas rendering surfaces, viewport scaling, and UI selection handles while maintaining zero DOM/React dependencies in domain engines.
