{
  "name": "@solospot/asset-manager-core",
  "version": "1.0.0",
  "description": "Core asset management interfaces and utilities for WEB FACTOR platform",
  "main": "./src/index.js",
  "types": "./src/index.d.ts",
  "files": [
    "src/**/*"
  ],
  "scripts": {
    "test": "vitest",
    "test:watch": "vitest watch",
    "test:coverage": "vitest run --coverage",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^20.0.0",
    "vitest": "^1.0.0",
    "typescript": "^5.0.0"
  }
}
