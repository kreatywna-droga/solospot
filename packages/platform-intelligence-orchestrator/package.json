{
  "name": "@web-factor/platform-intelligence-orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "Master orchestrator for @web-factor Platform Engineering Intelligence packages — read-only aggregation, deduplication, severity normalization, and unified Platform Health Score generation.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
