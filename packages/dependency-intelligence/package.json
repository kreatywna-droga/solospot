{
  "name": "@web-factor/dependency-intelligence",
  "version": "0.1.0",
  "private": true,
  "description": "Static analysis of monorepo dependency graphs, dependency cycles, version conflicts, unused dependencies, and ecosystem health.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
